# Arquivo de exemplo para configuração de ambiente
# Copie este arquivo para .env e preencha com seus valores reais.
# O arquivo .env NÃO deve ser commitado no Git.

# --- Credenciais Firebird ---
FIREBIRD_HOST="localhost"
FIREBIRD_PORT="3050"
FIREBIRD_DB_PATH="C:/Projetos/Dados.fdb" # Caminho completo para seu arquivo .fdb
FIREBIRD_USER="SYSDBA"
FIREBIRD_PASSWORD="M@nagers2023 # Substitua pela sua senha real
FIREBIRD_CHARSET="WIN1252" # Ajuste se necessário (ex: UTF8)

# --- Hugging Face (Opcional, mas recomendado) ---
# Necessário para baixar modelos Llama 3 ou privados
# HF_TOKEN="seu_token_huggingface_aqui"

# --- Configurações Opcionais Modelo/Fine-Tuning ---
# BASE_MODEL_NAME="meta-llama/Meta-Llama-3-8B-Instruct"
# SCHEMA_ADAPTER_PATH="./results-llama3-8b-chat-schema-adapter"
# LOG_LEVEL="INFO" 